{
  "name": "tws-maternity-db",
  "version": "2.0.0",
  "description": "Two Wild Souls Photography — Maternity content database & city listicle generator",
  "scripts": {
    "build": "node scripts/generate.js && node scripts/generate-cities.js",
    "build:guide": "node scripts/generate.js",
    "build:cities": "node scripts/generate-cities.js"
  },
  "engines": {
    "node": ">=16"
  }
}
