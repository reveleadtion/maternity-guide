{
  "name": "tws-maternity-db",
  "version": "1.0.0",
  "description": "Two Wild Souls Photography — Maternity content database & static site generator",
  "scripts": {
    "build": "node scripts/generate.js",
    "dev": "node scripts/generate.js && npx serve output"
  },
  "engines": {
    "node": ">=16"
  }
}
